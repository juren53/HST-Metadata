# HSTL Photo Framework Dependencies
# Version: 0.1.3d
# Python Requirement: 3.7+

# ============================================================================
# CORE FRAMEWORK DEPENDENCIES
# ============================================================================

# Configuration and data handling
PyYAML>=6.0                    # YAML configuration file parsing
pandas>=1.5.0                  # Data manipulation and CSV processing
pydantic>=1.10.0               # Configuration validation and data models

# Text processing and encoding
ftfy>=6.0.0                    # Text encoding fixes and Unicode normalization

# Progress tracking and terminal output
tqdm>=4.64.0                   # Progress bars for CLI operations
colorama>=0.4.4                # Cross-platform colored terminal output (optional)

# Logging enhancements
structlog>=22.0.0              # Enhanced structured logging

# ============================================================================
# IMAGE PROCESSING DEPENDENCIES
# ============================================================================

# Core image processing
Pillow>=9.0.0                  # Image processing and format conversions
PyExifTool>=0.5.0              # Python wrapper for ExifTool command-line utility

# ============================================================================
# GUI FRAMEWORK DEPENDENCIES
# ============================================================================

# Primary GUI framework
PyQt6>=6.0.0                   # Main GUI framework for the application

# Optional GUI utilities
wxPython>=4.1.0                # For csv_record_viewer.py utility (optional)

# ============================================================================
# EXCEL PROCESSING DEPENDENCIES
# ============================================================================

# Excel file reading and processing
openpyxl>=3.0.0                # For .xlsx Excel file reading
xlrd>=2.0.0                    # For legacy .xls Excel file reading

# ============================================================================
# DEVELOPMENT AND TESTING DEPENDENCIES
# ============================================================================

# Testing framework
pytest>=7.0.0                  # Unit testing framework
pytest-cov>=4.0.0              # Test coverage reporting

# ============================================================================
# PLATFORM-SPECIFIC DEPENDENCIES
# ============================================================================

# Windows-specific (auto-installed when needed)
# pywin32 - Windows COM automation for desktop shortcuts
# Note: This is auto-installed by create_desktop_shortcut.py if needed

# ============================================================================
# EXTERNAL DEPENDENCIES (Not installable via pip)
# ============================================================================

# ExifTool - Required for metadata operations (BOTH required):
# 1. ExifTool command-line tool - Download from: https://exiftool.org/
#    Must be installed separately and available in system PATH
# 2. PyExifTool Python wrapper - Included in requirements.txt above

# ============================================================================
# NOTES
# ============================================================================

# 1. Optional dependencies with fallbacks:
#    - colorama: If not available, logging works without colors
#    - pywin32: Auto-installed on Windows when creating desktop shortcuts
#    - wxPython: Only needed for csv_record_viewer.py utility
#
# 2. Authentication setup required:
#    - Google OAuth client secrets file for Google services integration
#    - Token pickle files are generated automatically on first use
#
# 3. Installation order recommendation:
#    1. Install core dependencies first (PyYAML, pandas, Pillow)
#    2. Install GUI framework (PyQt6)
#    3. Install Google services libraries
#    4. Install optional dependencies (colorama, structlog, wxPython)
# 5. Install external tools (ExifTool command-line + PyExifTool wrapper)
#    6. Set up Google authentication credentials
