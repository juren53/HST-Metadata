# Checking IPTC tags

To check IPTC tags of all TIFF files in a directory, run the following command from the Windows CMD window:
```
exiftool -iptc:all *.tif
```
To check tags in all JPEG files, substitute .tif for .jpg in the above command.
